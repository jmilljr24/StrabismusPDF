<!DOCTYPE html>
<html>
  <head>
    <title>Color Vans Plans</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload",
    defer: true %>

    <%= favicon_link_tag asset_path('airplane.png') %>
  </head>
  <body>
    <%= link_to "Home", root_path, class: "absolute bg-white m-2 top-0 left-0 hover:bg-gray-100 text-gray-800 font-semibold py-2 px-4 border border-gray-400 rounded shadow" %>
    <% flash.each do |type, msg| %>
        <div class="mb-3 inline-flex justify-center w-full items-center rounded-lg bg-primary-100 px-6 py-5 text-base text-primary-700">
        <%= msg %>
        </div>
    <% end %>
 
    <a href="https://www.buymeacoffee.com/vansrvcolorcode" class="absolute top-0
    right-0 m-2 p-2 shadow-xl bg-[rgb(80,124,92)]
hover:bg-gray-400 focus:shadow-outline
    focus:outline-none text-white font-bold rounded min-w-max"> &#127866; Buy me a beer</a>
    <div class="flex justify-center">
      <%= yield %>
    </div>
    <script src="../path/to/flowbite/dist/flowbite.min.js"></script>
  </body>
  <footer>
  </footer>
</html>
